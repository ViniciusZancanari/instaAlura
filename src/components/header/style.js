import {StyleSheet} from "react-native"

const screenStyle = StyleSheet.create ({
    userPicture:{
    width:40,
    height:40,
    borderRadius:30,
    margin:10
    },

    header:{
      flexDirection:'row',
      alignItems:'center'
    }
  })

export default screenStyle; 