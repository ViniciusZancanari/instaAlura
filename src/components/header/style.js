import {StyleSheet} from "react-native"

const screenStyle = StyleSheet.create ({
    userPicture:{
    width:45,
    height:45,
    borderRadius:30,
    margin:10
    },

    header:{
      flexDirection:'row',
      alignItems:'center'
    }
  })

export default screenStyle; 