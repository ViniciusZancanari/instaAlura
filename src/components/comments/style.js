import { StyleSheet } from "react-native";

const screenStyle = StyleSheet.create({
    inline: {
        flexDirection: "row",
        alignItems: "center"
    },
    imgSend: {
        width: 30,
        height: 30,
        marginRight: 5
    }
})

export default screenStyle;